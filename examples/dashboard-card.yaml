# Minimal example: add a Print PDF tile to any dashboard.
# After installing the integration, add to any view's cards: list.
#
# The card script is registered globally by the integration — you do NOT
# need a `resources:` block on the dashboard.

type: custom:ipp-print-upload-card
title: Print PDF
