[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
# Ruff defaults (pyflakes + a pycodestyle subset); no import-sorting to keep
# the existing import style untouched.
select = ["E4", "E7", "E9", "F"]
