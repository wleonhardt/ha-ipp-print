{
  "config": {
    "step": {
      "user": {
        "title": "IPP printer",
        "description": "Connect to a network printer via IPP.",
        "data": {
          "host": "Hostname or IP",
          "port": "Port",
          "use_tls": "Use TLS",
          "user": "User",
          "password": "Password",
          "verify_tls": "Verify TLS certificate",
          "relaxed_ciphers": "Allow legacy cipher suites"
        },
        "data_description": {
          "port": "443 for IPPS, 631 for IPP, 80 for plain HTTP.",
          "use_tls": "On for IPPS/HTTPS printers, off for plain IPP.",
          "user": "Sent as the IPP requesting-user-name. `anonymous` works for most printers.",
          "password": "Only if the printer requires HTTP basic auth.",
          "verify_tls": "Leave off for self-signed certificates (most consumer printers).",
          "relaxed_ciphers": "Enable if the log shows SSLV3_ALERT_HANDSHAKE_FAILURE (some HP LaserJets only offer non-PFS ciphers)."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the printer. Check host/port/TLS settings. Detail: {error_detail}"
    },
    "abort": {
      "already_configured": "This printer is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "IPP printer options",
        "data": {
          "host": "Hostname or IP",
          "port": "Port",
          "use_tls": "Use TLS",
          "user": "User",
          "password": "Password",
          "verify_tls": "Verify TLS certificate",
          "relaxed_ciphers": "Allow legacy cipher suites"
        },
        "data_description": {
          "port": "443 for IPPS, 631 for IPP, 80 for plain HTTP.",
          "use_tls": "On for IPPS/HTTPS printers, off for plain IPP.",
          "user": "Sent as the IPP requesting-user-name. `anonymous` works for most printers.",
          "password": "Only if the printer requires HTTP basic auth.",
          "verify_tls": "Leave off for self-signed certificates (most consumer printers).",
          "relaxed_ciphers": "Enable if the log shows SSLV3_ALERT_HANDSHAKE_FAILURE (some HP LaserJets only offer non-PFS ciphers)."
        }
      }
    }
  }
}
